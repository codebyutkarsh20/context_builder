# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.venv/
venv/
*.egg-info/
dist/
build/

# Environment — never commit secrets
.env
.env.local
.env.*.local

# Node
node_modules/
dist/
build/

# macOS
.DS_Store

# Local context output
/tmp/context_builder/

# Docker volumes
neo4j_data/

# Runtime data (generated at runtime, not source)
data/

# Claude Code / gstack skills (vendored tooling)
.claude/
AGENTS.md
.gstack/

# Agent runtime lock file
.agent_lock

# Research / planning docs (not source code)
AI_Deploy_Agent_v3_Research.md
PRD.md

# Eval runtime artifacts (cloned repos + result traces)
backend/eval/repos/
backend/eval/results/

# IDE / AI tool config files (local tooling, not project config)
.cursorrules
.windsurfrules
.mcp.json
.opencode.json
GEMINI.md

# SWE-bench raw download (large, regenerated via CLI)
backend/eval/swe_bugs.json

# Eval subset datasets (generated per-session, not source)
eval/next*.json
eval/sympy_only.json

# Temporary analysis docs (not project docs)
REMAINING_BUGS.md
SWE_BENCH_UI_TEST_BUGS.md

# Internal planning / research / manifest docs (not for end users)
agent_docs/
docs/superpowers/
docs/coding-agents-research-*.md

# Agent runtime config (auto-generated per repo)
.agent_config.json

# Eval one-off retry artifacts
eval/*_retry.json
